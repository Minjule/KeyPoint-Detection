path: keypoint_detection  # dataset root dir
train: train\images  # train images (relative to 'path') 4 images
val: validation\images  # val images (relative to 'path') 4 images
test: test\images # test images (optional)

# Keypoints
kpt_shape: [3, 2]  # number of keypoints, number of dims (2 for x,y or 3 for x,y,visible)
flip_idx: [0, 1, 2]

# Classes
names:
  0: rear_tire
  1: front_tire
  2: body